export var getVirtualScrollerOptions = function (_a) {
    var itemsLength = _a.itemsLength, _b = _a.maxVisibleItems, maxVisibleItems = _b === void 0 ? 5 : _b, _c = _a.itemHeight, itemHeight = _c === void 0 ? 38 : _c, _d = _a.extraHeight, extraHeight = _d === void 0 ? 24 : _d, _e = _a.lazy, lazy = _e === void 0 ? false : _e, onLazyLoad = _a.onLazyLoad;
    if (itemsLength === 0) {
        return undefined;
    }
    var visibleItems = Math.min(itemsLength, maxVisibleItems);
    var options = {
        itemSize: itemHeight,
        scrollHeight: "".concat(visibleItems * itemHeight + extraHeight, "px"),
    };
    // Lazy loading support
    if (lazy) {
        options.lazy = true;
        if (onLazyLoad) {
            options.onLazyLoad = onLazyLoad;
        }
    }
    return options;
};
//# sourceMappingURL=autoCompleteVirtualScroll.js.map