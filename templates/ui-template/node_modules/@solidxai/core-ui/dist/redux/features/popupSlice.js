var _a;
// store/popupSlice.ts
import { createSlice } from '@reduxjs/toolkit';
var initialState = {
    isOpen: false,
    event: null,
};
var popupSlice = createSlice({
    name: 'popup',
    initialState: initialState,
    reducers: {
        openPopup: function (state, action) {
            state.isOpen = true;
            state.event = action.payload;
        },
        closePopup: function (state) {
            state.isOpen = false;
            state.event = null;
        },
    },
});
export var openPopup = (_a = popupSlice.actions, _a.openPopup), closePopup = _a.closePopup;
export default popupSlice.reducer;
//# sourceMappingURL=popupSlice.js.map