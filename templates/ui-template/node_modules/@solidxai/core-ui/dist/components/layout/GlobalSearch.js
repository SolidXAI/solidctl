import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { usePathname } from "../../hooks/usePathname";
import { IconField } from 'primereact/iconfield';
import { InputIcon } from 'primereact/inputicon';
import { InputText } from 'primereact/inputtext';
export var GlobalSearch = function () {
    var path = usePathname();
    // const placeholderMap: { [key: string]: string } = {
    //     '/menu': 'Search Menus',
    //     '/category': 'Search Categories',
    //     '/menu-item': 'Search Menu Items',
    //     '/cms-banner-image': 'Search CMS Banner Images',
    //     '/article': 'Search Articles',
    // };
    // Find the matching placeholder dynamically
    // const placeholder = Object.keys(placeholderMap).find(key => pathname.includes(key)) || 'Search';
    var getPlaceholder = function () {
        var dynamicSegment = path.split('/').slice(-2, -1)[0]; // Extracts the second-to-last segment
        return "Search ".concat(dynamicSegment.replace(/-/g, ' ').replace(/\b\w/g, function (char) { return char.toUpperCase(); }));
    };
    return (_jsx("div", { className: 'absolute left-50', style: { transform: 'translateX(-50%)' }, children: _jsxs(IconField, { iconPosition: "left", children: [_jsx(InputIcon, { className: "pi pi-search" }), _jsx(InputText, { "v-model": "value1", placeholder: getPlaceholder(), 
                    // placeholder={placeholderMap[placeholder] || 'Search'}
                    className: 'border-cyan-200 border-1 max-h-2rem w-25rem' })] }) }));
};
//# sourceMappingURL=GlobalSearch.js.map