import { jsx as _jsx } from "react/jsx-runtime";
import MarkdownViewer from '../../../components/common/MarkdownViewer';
var isMarkupLanguage = function (text) {
    if (!text || typeof text !== 'string')
        return false;
    var markdownPatterns = [
        /^#{1,6}\s+/m,
        /\*\*.*?\*\*/,
        /\*.*?\*/,
        /`.*?`/,
        /```[\s\S]*?```/,
        /^\s*[-*+]\s+/m,
        /^\s*\d+\.\s+/m,
        /\[.*?\]\(.*?\)/,
        /^\s*\|.*\|/m,
        /^\s*>\s+/m,
    ];
    // Check for HTML tags
    var htmlPattern = /<\/?[a-z][\s\S]*?>/i;
    return markdownPatterns.some(function (pattern) { return pattern.test(text); }) || htmlPattern.test(text);
};
export var SolidChatterCustomMessage = function (_a) {
    var message = _a.message;
    var isMarkup = isMarkupLanguage(message);
    if (isMarkup) {
        return (_jsx("div", { className: "custom-message-wrapper", children: _jsx(MarkdownViewer, { data: message }) }));
    }
    return (_jsx("p", { className: 'm-0 text-sm', children: message }));
};
//# sourceMappingURL=SolidChatterCustomMessage.js.map