import { jsx as _jsx, Fragment as _Fragment } from "react/jsx-runtime";
import { AvatarWidget } from '../../../../components/core/common/AvatarWidget';
export var SolidManyToOneRelationAvatarListWidget = function (_a) {
    var _b, _c;
    var rowData = _a.rowData, solidListViewMetaData = _a.solidListViewMetaData, fieldMetadata = _a.fieldMetadata, column = _a.column;
    var manyToOneFieldData = rowData[column.attrs.name];
    // This is the userkey that will be present within the rowData.
    if (manyToOneFieldData) {
        // Since this is a many-to-one field, we fetch the user key field of the associated model.
        var userKeyField = (_c = (_b = fieldMetadata === null || fieldMetadata === void 0 ? void 0 : fieldMetadata.relationModel) === null || _b === void 0 ? void 0 : _b.userKeyField) === null || _c === void 0 ? void 0 : _c.name;
        var manyToManyColVal = [manyToOneFieldData[userKeyField]];
        // TODO: change this to use an anchor tag so that on click we open that entity form view. 
        return (_jsx(_Fragment, { children: manyToManyColVal.length > 0 &&
                manyToManyColVal.map(function (m) {
                    return _jsx(AvatarWidget, { value: m });
                }) }));
    }
    else {
        return _jsx("span", {});
    }
};
//# sourceMappingURL=SolidManyToOneRelationAvatarListWidget.js.map