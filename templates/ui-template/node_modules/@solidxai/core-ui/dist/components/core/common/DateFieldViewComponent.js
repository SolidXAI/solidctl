import { Fragment as _Fragment, jsx as _jsx } from "react/jsx-runtime";
import dayjs from "dayjs";
export var DateFieldViewComponent = function (_a) {
    var value = _a.value, format = _a.format, _b = _a.fallback, fallback = _b === void 0 ? "-" : _b;
    if (!value) {
        return _jsx(_Fragment, { children: fallback });
    }
    // Normalize value to Date
    var date = value instanceof Date ? value : new Date(value);
    // Invalid date → render raw value
    if (isNaN(date.getTime())) {
        return _jsx(_Fragment, { children: String(value) });
    }
    // Explicit format → dayjs
    if (format) {
        return _jsx(_Fragment, { children: dayjs(date).format(format) });
    }
    // Default → browser locale
    return _jsx(_Fragment, { children: date.toLocaleDateString() });
};
//# sourceMappingURL=DateFieldViewComponent.js.map