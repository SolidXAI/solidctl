import { jsx as _jsx } from "react/jsx-runtime";
import parse from 'html-react-parser';
// import Handlebars from "handlebars/dist/handlebars";
import * as Handlebars from "handlebars";
export var CustomHtml = function (_a) {
    var field = _a.field, formData = _a.formData, viewMetadata = _a.viewMetadata, fieldsMetadata = _a.fieldsMetadata;
    var type = field.type, attrs = field.attrs, body = field.body, children = field.children;
    // TODO: null check on this field required, show a proper error message on the UI suggesting that you are using a CustomHtml widget, however the html is not specified in the layout.
    var html = attrs.html;
    // TODO: run dompurify to avoid XSS attacks...
    // TODO: assume the html is a handlebars template, we try to do variable replacement on it using formData.
    var template = Handlebars.compile(html);
    return (_jsx("div", { className: attrs.className, children: parse(template(formData)) }));
};
//# sourceMappingURL=CustomHtml.js.map