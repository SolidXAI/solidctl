import SolidBigintField from "./fields/SolidBigintField";
import SolidFloatField from "./fields/SolidFloatField";
import SolidDecimalField from "./fields/SolidDecimalField";
import SolidShortTextField from "./fields/SolidShortTextField";
import SolidLongTextField from "./fields/SolidLongTextField";
import SolidRichTextField from "./fields/SolidRichTextField";
import SolidBooleanField from "./fields/SolidBooleanField";
import SolidDateField from "./fields/SolidDateField";
import SolidDatetimeField from "./fields/SolidDatetimeField";
import SolidTimeField from "./fields/SolidTimeField";
import SolidRelationField from "./fields/SolidRelationField";
import SolidMediaSingleField from "./fields/SolidMediaSingleField";
import SolidMediaMultipleField from "./fields/SolidMediaMultipleField";
import SolidSelectionStaticField from "./fields/SolidSelectionStaticField";
import SolidSelectionDynamicField from "./fields/SolidSelectionDynamicField";
import SolidComputedField from "./fields/SolidComputedField";
import SolidExternalIdField from "./fields/SolidExternalIdField";
import SolidUuidField from "./fields/SolidUuidField";
import SolidIntField from "./fields/SolidIntField";
export var getNumberOfInputs = function (matchMode) {
    switch (matchMode) {
        case '$between':
            return 2;
        case '$in':
        case '$notIn':
            return null;
        case '$startsWith':
        case '$contains':
        case '$notContains':
        case '$endsWith':
        case '$equals':
        case '$notEquals':
        case '$lt':
        case '$lte':
        case '$gt':
        case '$gte':
            return 1;
        case '$null':
        case '$notNull':
            return 0;
        default:
            return 1; // Default to single input if no specific match is found
    }
};
export var SolidFilterFields = function (_a) {
    var fieldMetadata = _a.fieldMetadata, onChange = _a.onChange, index = _a.index, rule = _a.rule;
    if (fieldMetadata) {
        // And finally we can implement additional switching logic based on certain special fields. 
        if (fieldMetadata.name === 'id') {
            return SolidIntField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'int') {
            return SolidIntField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'bigint') {
            return SolidBigintField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'float') {
            return SolidFloatField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'decimal') {
            return SolidDecimalField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'shortText') {
            return SolidShortTextField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'longText') {
            return SolidLongTextField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'richText') {
            return SolidRichTextField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'boolean') {
            return SolidBooleanField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'date') {
            return SolidDateField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'datetime') {
            return SolidDatetimeField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'time') {
            return SolidTimeField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'relation') {
            return SolidRelationField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'mediaSingle') {
            return SolidMediaSingleField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'mediaMultiple') {
            return SolidMediaMultipleField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'selectionStatic') {
            return SolidSelectionStaticField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'selectionDynamic') {
            return SolidSelectionDynamicField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'computed') {
            return SolidComputedField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'externalId') {
            return SolidExternalIdField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
        if (fieldMetadata.type === 'uuid') {
            return SolidUuidField({ fieldMetadata: fieldMetadata, onChange: onChange, index: index, rule: rule });
        }
    }
};
//# sourceMappingURL=SolidFilterFields.js.map