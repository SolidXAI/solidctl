import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import MarkdownViewer from "../../../../../components/common/MarkdownViewer";
import { Accordion, AccordionTab } from "primereact/accordion";
import { Button } from "primereact/button";
import { TabView, TabPanel } from "primereact/tabview";
import ReactCodeMirror, { EditorView, oneDark } from "@uiw/react-codemirror";
import { javascript } from "@codemirror/lang-javascript";
export var SolidAiInteractionMetadataFieldFormWidget = function (_a) {
    var _b, _c, _d, _e;
    var formik = _a.formik, fieldContext = _a.fieldContext;
    var fieldMetadata = fieldContext.fieldMetadata;
    var fieldLayoutInfo = fieldContext.field;
    var fieldLabel = (_b = fieldLayoutInfo.attrs.label) !== null && _b !== void 0 ? _b : fieldMetadata.displayName;
    var userKeyFieldName = (_d = (_c = fieldMetadata.relationModel) === null || _c === void 0 ? void 0 : _c.userKeyField) === null || _d === void 0 ? void 0 : _d.name;
    var value = formik.values[fieldLayoutInfo.attrs.name];
    return (_jsx("div", { className: "mt-2 flex-column", children: _jsxs(TabView, { children: [_jsx(TabPanel, { header: "Chunks", children: _jsx("div", { className: "solid-layout-accordion", children: _jsx(Accordion, { multiple: true, expandIcon: "pi pi-chevron-down", collapseIcon: "pi pi-chevron-up", activeIndex: [0], children: (_e = value === null || value === void 0 ? void 0 : value.chunks) === null || _e === void 0 ? void 0 : _e.map(function (c) {
                                var processedText = c.text || '';
                                var meta = c.metadata || {};
                                // 🧠 Identify type
                                var isDoc = !!meta.path || meta.source === "docusaurus";
                                var isModule = meta.type === "module";
                                var isModel = meta.type === "model";
                                // 🧹 Clean text for docs
                                if (isDoc) {
                                    processedText = processedText
                                        .replace(/^Document Title:\s*/i, "")
                                        .replace(/^Text:\s*/i, "")
                                        .trim();
                                }
                                // 🧩 Extract "Full model metadata json" if present
                                if (isModel) {
                                    var match = processedText.match(/Full model metadata json:\s*\n?({[\s\S]+})/);
                                    if (match) {
                                        try {
                                            // Format the JSON nicely
                                            var pretty = JSON.stringify(JSON.parse(match[1]), null, 2);
                                            // Replace the raw JSON text with a fenced code block
                                            processedText = processedText.replace(match[0], "**Full model metadata json:**\n\n```json\n".concat(pretty, "\n```"));
                                        }
                                        catch (_a) {
                                            // Fallback: wrap raw text if JSON invalid
                                            processedText = processedText.replace(match[0], "**Full model metadata json (raw):**\n\n```\n".concat(match[1], "\n```"));
                                        }
                                    }
                                }
                                // 🏷️ Header label
                                var headerTitle = "Unknown";
                                if (isModule) {
                                    headerTitle = "Module: ".concat(meta.moduleName, " (").concat(meta.modelCount, " models)");
                                }
                                else if (isModel) {
                                    headerTitle = "Model: ".concat(meta.modelName, " (").concat(meta.fieldCount, " fields)");
                                }
                                else if (isDoc) {
                                    headerTitle = "Doc: ".concat(meta.title || meta.doc_title || meta.path);
                                }
                                // 🌐 External link (docs only)
                                var openDoc = function (e) {
                                    e.stopPropagation();
                                    if (isDoc) {
                                        var cleanPath = (meta.path || "").replace(/\.md$/, "");
                                        var url = "https://docs.solidxai.com/docs/".concat(cleanPath);
                                        window.open(url, "_blank");
                                    }
                                };
                                return (_jsx(AccordionTab, { header: _jsxs("div", { className: "flex justify-between items-center w-full", children: [_jsx("span", { className: "font-medium", children: headerTitle }), isDoc && (_jsx(Button, { icon: "pi pi-external-link", rounded: true, text: true, outlined: true, style: { height: "1.5rem" }, onClick: openDoc }))] }), children: _jsx("div", { className: "shadow-1 bg-bluegray-50 p-3", children: _jsx(MarkdownViewer, { data: processedText }) }, c.id) }, c.id));
                            }) }) }) }), _jsx(TabPanel, { header: "System Prompt", children: (value === null || value === void 0 ? void 0 : value.system_prompt) && (_jsx("div", { children: _jsx(MarkdownViewer, { data: value.system_prompt }) })) }), _jsx(TabPanel, { header: "User Prompt", children: (value === null || value === void 0 ? void 0 : value.user_prompt) && (_jsx("div", { children: _jsx(MarkdownViewer, { data: value.user_prompt }) })) }), _jsx(TabPanel, { header: "Token Usage", children: (value === null || value === void 0 ? void 0 : value.tokenUsage) && (_jsx("div", { className: "p-3 ", style: { width: '100%' }, children: _jsx(ReactCodeMirror, { value: JSON.stringify(value.tokenUsage, null, 2), style: { fontSize: '10px' }, theme: oneDark, extensions: [javascript(), EditorView.lineWrapping] }) })) })] }) }));
};
//# sourceMappingURL=SolidAiInteractionMetadataFieldFormWidget.js.map