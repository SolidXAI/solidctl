import { useEffect, useState } from "react";
import { usePathname } from "../../hooks/usePathname";
import { useLazyGetAuthSettingsQuery } from "../../redux/api/solidSettingsApi";
export var SolidThemeProvider = function () {
    var pathname = usePathname();
    var _a = useLazyGetAuthSettingsQuery(), trigger = _a[0], solidSettingsData = _a[1].data;
    useEffect(function () {
        if (pathname.includes("/auth/")) {
            trigger("");
        }
    }, [pathname, trigger]);
    var _b = useState("solid-light-purple"), theme = _b[0], setTheme = _b[1];
    useEffect(function () {
        var _a, _b;
        if (pathname.includes("/auth/") && ((_a = solidSettingsData === null || solidSettingsData === void 0 ? void 0 : solidSettingsData.data) === null || _a === void 0 ? void 0 : _a.authPagesTheme)) {
            var selectedTheme = ((_b = solidSettingsData === null || solidSettingsData === void 0 ? void 0 : solidSettingsData.data) === null || _b === void 0 ? void 0 : _b.authPagesTheme) === "dark" ? "solid-dark-purple" : "solid-light-purple";
            setTheme(selectedTheme);
        }
        else {
            setTheme("solid-light-purple");
        }
    }, [pathname, solidSettingsData]);
    useEffect(function () {
        // Find or create <link> element
        var themeLink = document.getElementById("theme-css");
        // if (!themeLink) {
        //     themeLink = document.createElement("link");
        //     themeLink.id = "theme-css";
        //     themeLink.rel = "stylesheet";
        //     document.head.appendChild(themeLink);
        // }
        // Update theme link dynamically
        themeLink.href = "/themes/".concat(theme, "/theme.css");
    }, [theme]);
    return null;
};
//# sourceMappingURL=SolidThemeProvider.js.map