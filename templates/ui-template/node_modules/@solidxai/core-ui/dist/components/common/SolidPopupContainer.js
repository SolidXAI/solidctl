var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { jsx as _jsx } from "react/jsx-runtime";
// components/PopupContainer.tsx
import { useSelector, useDispatch } from 'react-redux';
import { closePopup } from '../../redux/features/popupSlice';
import { Dialog } from 'primereact/dialog';
import { getExtensionComponent } from '../../helpers/registry';
var SolidPopupContainer = function () {
    var _a = useSelector(function (state) { return state.popup; }), isOpen = _a.isOpen, event = _a.event;
    var dispatch = useDispatch();
    if (!isOpen)
        return null;
    var DynamicComponent = getExtensionComponent(event === null || event === void 0 ? void 0 : event.action);
    return (_jsx(Dialog, { visible: isOpen, onHide: function () { return dispatch(closePopup()); }, closable: false, style: { width: event.popupWidth ? event.popupWidth : '50vw' }, modal: true, headerClassName: 'p-0', contentClassName: 'p-0', contentStyle: { borderRadius: 6 }, className: 'solid-common-dialog', children: DynamicComponent && _jsx(DynamicComponent, __assign({}, event)) }));
};
export default SolidPopupContainer;
//# sourceMappingURL=SolidPopupContainer.js.map