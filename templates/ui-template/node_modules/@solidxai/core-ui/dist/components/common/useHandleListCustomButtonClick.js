var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { useDispatch } from "react-redux";
import { getExtensionFunction } from "../../helpers/registry";
import { openPopup } from "../../redux/features/popupSlice";
import { ERROR_MESSAGES } from "../../constants/error-messages";
export var useHandleListCustomButtonClick = function () {
    var dispatch = useDispatch();
    return function (buttonAttrs, event) {
        var action = buttonAttrs.action, openInPopup = buttonAttrs.openInPopup;
        if (openInPopup === true) {
            var eventData = __assign(__assign({}, event), buttonAttrs);
            dispatch(openPopup(eventData));
        }
        else {
            var eventData = __assign(__assign({}, event), buttonAttrs);
            var dynamicFunction = getExtensionFunction(action);
            if (!dynamicFunction) {
                console.error(ERROR_MESSAGES.ACTION_FUNCTION(action));
                return;
            }
            dynamicFunction(eventData);
        }
    };
};
//# sourceMappingURL=useHandleListCustomButtonClick.js.map